config BR2_PACKAGE_CAMERA_SENSOR
    bool "camera-sensor"
    depends on BR2_aarch64
    help
      IMX415 V4L2 sub-device driver and peripheral drivers
      (UART sensor, GPIO trigger) for Edge AI Camera.